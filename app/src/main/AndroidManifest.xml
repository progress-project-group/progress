<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.progress_android">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
<!--        <activity-->
<!--            android:name=".MonthlyPlan.EventAddActivity"-->
<!--            android:theme="@style/MonthlyTheme"></activity>-->
<!--        <activity-->
<!--            android:name=".MonthlyPlan.MonthlyPlanActivity"-->
<!--            android:theme="@style/MonthlyTheme" />-->
        <activity android:name=".LongTermSchedulePagerActivity" android:parentActivityName=".LongTermScheduleListActivity"></activity>
        <activity android:name=".LongTermScheduleListActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".DailySummaryActivity"></activity>
        <activity android:name=".DailyPlanActivity" />
        <activity android:name=".EventListActivity" />
        <activity android:name=".TimelineActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

</manifest>